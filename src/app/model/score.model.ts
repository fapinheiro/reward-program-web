import { PostalCode } from './postal-code.model';
import { DecimalPipe } from '@angular/common';

export class Score {
    
    codScore: number; 
    goodType: string;
    creditMin: number;
    creditMax: number;
    instMin: number;
    instMax: number;
    score: number;
    creationAt: string;
    updatedAt: string;
    
}